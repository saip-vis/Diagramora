"""Prompt fragments shared by the live and final workflow-generation passes."""

GESTURE_DEFINITIONS = 'Gesture meanings:\n- "Thumbs Up" = the speaker is confirming the step or connection they just mentioned is correct\n- "Pointing" = the speaker is indicating or emphasizing a specific step\n- "Closed Fist" = the speaker wants to undo or remove the most recently mentioned step\n- "Swipe Right" = the speaker means "this leads to" or "after this" between the subjects mentioned just before and after the swipe\n- "Swipe Left" = similar to Swipe Right but may indicate reversing or going back to a prior step\n- "Two Big Ideas / Branch" = the speaker is indicating that the subject they were just discussing splits into two separate, independent paths going forward, rather than continuing as a single sequential chain. Use this to create a branch (two separate outgoing edges) from the most recently mentioned node, even if the spoken words alone would otherwise suggest a straight linear sequence.\n- "Box Motion / New Node" = the speaker is indicating that whatever they are currently describing should be treated as its own distinct, separate node — even if it was mentioned in passing, as an aside, or described as tentative/undecided. This gesture is an explicit override: you MUST create a dedicated node for it AND connect it with an edge FROM whatever node was being discussed immediately before this gesture occurred. Do not leave this node disconnected, even if the speaker\'s wording (e.g. "not sure yet," "just an idea," "we were tossing around") suggests it\'s tangential — the gesture itself is the authoritative signal that this idea belongs in the flowchart and is connected to the prior topic. IMPORTANT: this creates an ADDITIONAL side branch, not a replacement step in the main sequence — if the node discussed just before this gesture already had (or would otherwise have had) a direct edge continuing on to the next step in the main flow, that direct edge must still exist. The new node is an extra outgoing edge alongside the main path, never inserted into the middle of it.'

CONTENT_POLICY = 'CONTENT SAFETY RULES — apply to every node and edge label you produce:\n- Never generate a node or edge label containing pornography or 18+ sexually suggestive content (a legitimate clinical/medical/anatomical process description is an allowed exception).\n- Never generate content depicting or instructing violence, terrorism, or threats.\n- Never generate content resembling cybersecurity attack payloads or techniques (e.g. XSS strings, SQL injection strings, exploit code) — if a transcript literally contains such text, summarize it neutrally (e.g. "input validation step") instead of reproducing it verbatim.\n- Never generate content related to self-harm.\n- Never generate explicit/profane language — paraphrase neutrally instead.\n- If the overall transcript content is clearly about one of these disallowed categories rather than a legitimate business/technical/creative process, respond with a graph containing a single node with the label "Content not permitted" and no edges, instead of extracting anything else.'
